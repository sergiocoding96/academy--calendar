# Required for auth and database
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: AI Tournament Agent (Gemini)
GOOGLE_API_KEY=

# Optional: Scheduled tournament scraping (Vercel Cron)
# Set in Vercel dashboard; use same value in cron job Authorization header
CRON_SECRET=

# Optional: Slack daily digest (also used by notify-slack edge function)
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=

# Optional: Live external tournament search (ITF, etc.)
SCRAPFLY_API_KEY=
